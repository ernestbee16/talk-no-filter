{
  "name": "talk-no-filter-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "if [ -d \"backend\" ]; then cd backend; fi && npm install && npx prisma generate && npx tsc",
    "start": "if [ -d \"backend\" ]; then cd backend; fi && npm run start"
  }
}
